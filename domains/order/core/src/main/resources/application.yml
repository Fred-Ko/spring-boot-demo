spring:
  application:
    name: order-core

# 데이터베이스 설정 (예시)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true